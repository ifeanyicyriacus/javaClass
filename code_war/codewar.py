def get_unsorted_pairs(numlist, k):
    result = []
    for index in numlist:
        for index2 in numlist:
            f


create empty list [] abc
loop through the array x
    loop through the array y
        if arrray[x] is not k and array[y] is not k
            abc.append [x,y]

a list of repeated pairs

[x for x in repeatedPaires if x ]
new list []

for x in repeatedPaires
    x
    if
